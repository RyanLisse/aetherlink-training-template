# Project instructions

@intent.md
@progress.md
@AGENTS.md

`intent.md` and `progress.md` are project convention files; their presence at
the repository root does not load them independently. Keep these imports
explicit so the training outcome, current-state board, and contribution rules
are loaded together before changing an artifact.
